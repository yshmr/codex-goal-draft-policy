interface:
  display_name: "Goal Draft Policy"
  short_description: "Draft or review a Codex Goal contract before activation"
  default_prompt: "Draft a human-reviewable /goal completion contract and stop before activation."
policy:
  allow_implicit_invocation: true
