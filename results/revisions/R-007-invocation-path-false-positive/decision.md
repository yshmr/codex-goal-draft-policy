# R-007 decision

Decision: `ADOPT` the grader revision after `npm test` passes the new pure regression.

This decision concerns deterministic trace interpretation only. It does not convert any contaminated provider run into a valid result.
