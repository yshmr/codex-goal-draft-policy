# R-013 decision

Decision: `REJECT` the mechanical witness method.

Both cells were normal, but the predeclared candidate exact-action gate was false. Baseline absence, boundary, parity, hashes, public safety, and freshness do not substitute for that missing gate. The pair is immutable, no additional run is permitted, and valid paired smoke remains ineligible.
