# R-012 diagnosis

The candidate final response followed candidate-specific instructions, but output style and self-report were explicitly disallowed as invocation evidence. The structured JSONL contained no command read of the candidate `SKILL.md` and no test marker.

All shell reads in both conditions failed because the container kernel denied the bubblewrap namespace required by the fixed read-only sandbox. That explains why the predeclared source-read witness could not be established, but it does not permit reinterpreting the prose output as a pass.
