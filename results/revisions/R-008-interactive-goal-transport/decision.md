# R-008 decision

Status: `PENDING CLEAN RERUN`.

The revised interactive capture method must be committed before a fresh isolated `/goal` run. Adopt or reject the method only after that run is captured under a new manifest. Earlier failed and contaminated authorities remain unchanged.
