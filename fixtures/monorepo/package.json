{
  "name": "fixture-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "node --test packages/*/test/*.test.js",
    "test:api": "npm test --workspace packages/api"
  }
}
