# Approved synthetic evaluation Goal

Run `node score.mjs` against the frozen `cases.json` and record an adopt, reject, or inconclusive decision supported by its output. Do not change the cases or scoring rule after seeing the result.
