# Existing reviewed Goal

`/goal Decide whether to adopt the candidate parser using the frozen population, baseline, candidate, and scoring rules in EVAL.md. Preserve public APIs and do not change cases after seeing results. Verify correctness with go test ./.... Treat adopt, reject, or inconclusive as valid evidence-supported endings. Between attempts, address only the largest evidence gap. If a required case or runner is unavailable and no bounded alternative remains, report the missing input, attempted checks, and what would unblock the decision.`
