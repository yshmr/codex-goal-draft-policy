# Release boundary

Publishing requires a private signing certificate that is intentionally not present in this public fixture. Local packaging cleanup and validation may proceed without it. Release publication must stop and identify the missing certificate as the concrete blocker.
