# Python packaging fixture

The local validation surfaces are `python -m pytest` and `python -m compileall src`. A release signing step exists, but local tests and compilation do not need the signing certificate.
