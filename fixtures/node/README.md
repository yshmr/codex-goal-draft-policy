# Node service fixture

This synthetic service exposes a small arithmetic helper. The repository defines test and syntax-check commands, but it defines no latency, coverage, cost, or quality threshold.

Public API compatibility is required during the planned client migration.
